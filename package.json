{
  "name": "carrefour-api-tests",
  "version": "1.0.0",
  "scripts": {
  "test": "npx jest --runInBand --detectOpenHandles --coverage",
  "test:report": "npx jest --runInBand --detectOpenHandles && allure generate --clean && allure open"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2",
    "supertest": "^7.1.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "allure-commandline": "^2.29.0",
    "allure-js-commons": "^2.15.1",
    "jest": "^29.7.0",
    "jest-circus": "^29.7.0"
  }
}
